pipeline {
    agent {
        docker {
            image 'scala-herencia:dev'
            args '-v $PWD:/app -w /app'
        }
    }

    stages {
        stage('Compilar') {
            steps {
                sh 'sbt compile'
            }
        }

        stage('Test') {
            steps {
                sh 'sbt test'
                sh 'ls -l target/test-reports' 
            }
        }
    }

    post {
        always {
            junit 'target/test-reports/*.xml'
        }
    }
}

