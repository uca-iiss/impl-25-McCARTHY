pipeline {
    agent any

    stages {
        stage('Construcción y pruebas') {
            steps {
                sh 'docker build -t inyeccion-cpp .'
                sh 'docker run --rm inyeccion-cpp ctest --test-dir build'
            }
        }
        stage('Ejecución') {
            steps {
                sh 'docker run --rm inyeccion-cpp'
            }
        }
    }

    post {
        always {
            echo 'Pipeline completado.'
        }
    }
}
